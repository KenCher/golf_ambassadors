services:
  - type: web
    name: golf4us
    env: node
    buildCommand: npm install
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /mobile.html

# Made with Bob
